<app-header></app-header>
<div class="content">
  <div class="services-header">
    <p class="h1">Available Services</p>
  </div>
  <div class="table-container">
    <table mat-table [dataSource]="services" class="mat-elevation-z0 no-border">
      <!-- Icon Column -->
      <ng-container matColumnDef="icon">
        <th mat-header-cell *matHeaderCellDef> Icon </th>
        <td mat-cell *matCellDef="let service">
          <a [routerLink]="[service.route]">
            <img [src]="service.icon" class="service-icon" />
          </a>
        </td>
      </ng-container>

      <!-- Name Column -->
      <ng-container matColumnDef="name">
        <th mat-header-cell *matHeaderCellDef> Name </th>
        <td mat-cell *matCellDef="let service">
          <a [routerLink]="[service.route]">{{ service.name }}</a>
        </td>
      </ng-container>

      <!-- Description Column -->
      <ng-container matColumnDef="description">
        <th mat-header-cell *matHeaderCellDef> Description </th>
        <td mat-cell *matCellDef="let service"> {{ service.description }} </td>
      </ng-container>

      <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
      <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
    </table>
  </div>
</div>



.content {
  padding: 20px;
}

.services-header {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 150px; /* Adjust this height as needed */
  text-align: center;
}

.h1 {
  font-size: 2rem;
  color: #5c7e8d; /* Adjust this color if needed */
  margin: 0;
}

.table-container {
  overflow-x: auto; /* Ensure table is scrollable on smaller screens */
}

.mat-table {
  width: 100%;
  margin-top: 20px; /* Add some space between the header and the table */
  border-collapse: collapse;
}

.no-border .mat-header-row, .no-border .mat-row {
  border: none; /* Remove border for rows */
}

.no-border .mat-header-cell, .no-border .mat-cell {
  border: none; /* Remove border for cells */
}

.mat-cell, .mat-header-cell {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
}

.service-icon {
  width: 50px;
  height: 50px;
}

@media (max-width: 768px) {
  .h1 {
    font-size: 1.5rem;
  }

  .services-header {
    height: auto;
    padding: 20px 0;
  }

  .table-container {
    margin-top: 20px;
  }

  .mat-table {
    display: block; /* Make the table scrollable on small screens */
  }

  .mat-header-cell, .mat-cell {
    display: block; /* Ensure cells are block level */
    width: 100%; /* Make cells take full width */
    box-sizing: border-box;
    padding: 10px; /* Add padding for readability */
  }

  .mat-header-row, .mat-row {
    display: block; /* Make rows block level */
  }
}
