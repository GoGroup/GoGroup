.custom-tab-group {
  width: 100%;
}

.custom-tab .mat-tab-label {
  background-color: #5c7e8d; /* Your primary color */
  color: white;
  border: 1px solid #4a6a79; /* Slightly darker shade of your primary color */
  border-radius: 4px 4px 0 0; /* Rounded top corners */
  padding: 0.5em 1em; /* Padding for the tab labels */
  margin-right: 2px; /* Space between tab labels */
}

.custom-tab .mat-tab-label-active {
  background-color: white; /* Active tab background color */
  color: #5c7e8d; /* Active tab text color */
  border-bottom: none; /* Remove bottom border to merge with tab content */
  position: relative;
  top: 1px; /* Slightly elevate the active tab */
}

/* Adjust tab content border and padding */
.custom-tab-group .mat-tab-body-wrapper {
  border: 1px solid #4a6a79;
  border-top: none; /* Remove top border to merge with active tab */
  padding: 1em; /* Padding for the tab content */
}

.tab-content {
  background-color: black; /* Set background color to black */
  color: white; /* Ensure text is visible against black background */
  padding: 1em; /* Maintain padding */
}

.vertical-radio-group {
  display: flex;
  flex-direction: column;
}

.input-field {
  margin-top: 1em;
}

mat-form-field {
  width: 100%;
}

.action-button-container {
  display: flex;
  justify-content: center;
  margin-top: 1em; /* Add space between the card and the button */
}
